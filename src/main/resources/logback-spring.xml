<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Registrar el converter de enmascaramiento de PANs -->
    <conversionRule conversionWord="msg"
                    converterClass="com.ferromax.erp.util.PanMaskingConverter"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- En producción, agregar un FileAppender aquí con el mismo pattern -->

    <springProfile name="default,dev">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        <logger name="com.ferromax" level="INFO"/>
        <logger name="org.springframework.security" level="INFO"/>
        <logger name="org.hibernate.SQL" level="WARN"/>
    </springProfile>

    <springProfile name="prod">
        <root level="WARN">
            <appender-ref ref="CONSOLE"/>
        </root>
        <logger name="com.ferromax" level="INFO"/>
    </springProfile>

</configuration>
